
function refreshForm(innerHTML) {
    const checkTodoForm = document.querySelector("[data-checktodo-id='<%= @todo.id %>']");
    checkTodoForm.innerHTML = innerHTML;
  }

refreshForm('<%= j render "todos/showupdate", menu: @menu, todo: @todo %>');
