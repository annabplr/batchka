
function refreshForm(innerHTML) {
    const checkIngredientForm = document.querySelector("[data-checkingredient-id='<%= @ingredient.id %>']");
    checkIngredientForm.innerHTML = innerHTML;
  }
refreshForm('<%= j render "ingredients/showupdate", menu: @menu, ingredient: @ingredient %>');
